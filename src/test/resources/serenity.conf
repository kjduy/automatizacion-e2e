serenity {
    take.screenshots = FOR_FAILURES
    test.root = "orangehrm.features"
    logging = VERBOSE
}

webdriver {
    driver = chrome
    driver.capabilities {
        browserName = "chrome"
        acceptInsecureCerts = true
        "goog:chromeOptions" {
            args = ["remote-allow-origins=*", "--start-maximized", "--no-sandbox", "--disable-dev-shm-usage"]
        }
    }
    capabilities {
        browserName = "chrome"
        acceptInsecureCerts = true
        "goog:chromeOptions" {
            args = ["remote-allow-origins=*", "--start-maximized"]
        }
    }
}
